---
phase: 01-funda-o-execut-vel
plan: "02"
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/config/urls.py
  - src/leadstream/common/request_context.py
  - src/leadstream/common/logging.py
  - src/leadstream/common/health.py
  - src/leadstream/common/views.py
  - src/leadstream/common/urls.py
  - src/leadstream/integrations/appwrite.py
  - tests/test_health.py
  - tests/test_appwrite_adapter.py
  - tests/test_logging.py
autonomous: true
requirements:
  - FND-01
  - FND-05
  - FND-07
  - OPS-01
user_setup:
  - service: appwrite
    why: "Validar a instância self-hosted no diagnóstico de produção"
    env_vars:
      - APPWRITE_ENDPOINT
      - APPWRITE_PROJECT_ID
      - APPWRITE_API_KEY
    dashboard_config:
      - "Rotacionar a chave já compartilhada e criar uma chave exclusiva com escopo mínimo health.read; storage será ampliado apenas na fase de exportação."
must_haves:
  truths:
    - "Liveness nunca consulta dependências e readiness falha somente quando banco/migrations essenciais falham."
    - "Redis, RabbitMQ e Appwrite são diagnósticos independentes e nunca vazam detalhes de conexão."
    - "O adaptador Appwrite usa endpoint, project ID e chave apenas do ambiente e não participa do ORM."
    - "Toda requisição recebe ID de correlação e logs estruturados redigem dados sensíveis."
    - "OpenAPI versionado permite executar a rota real do workspace."
  artifacts:
    - path: src/leadstream/common/health.py
      provides: "Checks separados de vida, prontidão e dependências"
    - path: src/leadstream/integrations/appwrite.py
      provides: "Porta desacoplada para diagnóstico Appwrite"
    - path: src/leadstream/common/logging.py
      provides: "Logging JSON com redação"
    - path: src/config/urls.py
      provides: "Schema OpenAPI e documentação interativa"
  key_links:
    - from: src/leadstream/common/views.py
      to: src/leadstream/common/health.py
      via: "health services"
      pattern: "check_"
    - from: src/leadstream/integrations/appwrite.py
      to: environment
      via: "APPWRITE_ENDPOINT/PROJECT_ID/API_KEY"
      pattern: "APPWRITE_"
    - from: src/leadstream/common/request_context.py
      to: src/leadstream/common/logging.py
      via: "request_id contextvar"
      pattern: "request_id"
---

<objective>
Tornar a API diagnosticável e auditável sem expor infraestrutura: health checks com semântica correta, contrato OpenAPI, adaptador Appwrite e logs estruturados correlacionados.

Purpose: permitir operação segura no EasyPanel e comprovar a conectividade externa sem acoplar disponibilidade opcional à API principal.
Output: rotas de health/docs, adapter Appwrite, contexto de request, redação de logs e testes de falhas.
</objective>

<execution_context>
@C:/Users/lxleo/.codex/skills/gsd-execute-phase/SKILL.md
@C:/Users/lxleo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/lxleo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-funda-o-execut-vel/01-CONTEXT.md
@.planning/phases/01-funda-o-execut-vel/01-RESEARCH.md
@.planning/phases/01-funda-o-execut-vel/01-01-SUMMARY.md
</context>

<threat_model>
Assets: Appwrite API key, URLs internos, traces, request metadata e disponibilidade da API.
Trust boundaries: gateway para middleware; health endpoint para dependências; adapter para Appwrite HTTPS.
Threats and controls:
- Reconhecimento da infraestrutura por health: respostas públicas limitadas a estado/código, sem host, erro bruto ou trace.
- Segredos/PII em logs: filtro recursivo por chaves sensíveis, truncamento e testes de não exposição.
- Health como vetor de indisponibilidade: timeouts curtos; liveness sem I/O; checks opcionais separados.
- TLS inseguro no Appwrite: HTTPS obrigatório em produção e verificação TLS não desabilitável.
- Appwrite comprometendo prontidão básica: check separado e não bloqueante (D-08 a D-11).
Verification: fixtures simulam timeout, 401 e falha de dependência; testes afirmam ausência de detalhes.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implementar semântica de health e diagnósticos</name>
  <files>src/leadstream/common/health.py, src/leadstream/common/views.py, src/leadstream/common/urls.py, src/config/urls.py, tests/test_health.py</files>
  <read_first>src/config/settings/base.py, src/config/urls.py, .planning/phases/01-funda-o-execut-vel/01-CONTEXT.md, .planning/research/PITFALLS.md</read_first>
  <action>Implemente `/health/live` sem qualquer I/O; `/health/ready` executa consulta mínima ao PostgreSQL e verifica se migrations essenciais estão aplicadas; `/health/dependencies` consulta Redis, RabbitMQ e Appwrite separadamente com timeout curto. Readiness não depende de Appwrite/Redis/broker nesta fase, e respostas só informam `ok`, `degraded` ou `unavailable`, nunca exceção, endereço ou credencial (D-08, D-09, D-10, D-11). Use 200 para live/ready saudáveis e 503 para readiness bloqueada.</action>
  <verify>`pytest tests/test_health.py -q` passa cobrindo processo vivo, banco pronto, banco indisponível e dependência opcional degradada.</verify>
  <acceptance_criteria>O teste comprova que `/health/live` não chama banco/cache/HTTP; `/health/ready` retorna 503 quando o banco falha; `/health/dependencies` continua respondendo com campos sanitizados.</acceptance_criteria>
  <done>Probes têm responsabilidades distintas e podem ser usadas pelo EasyPanel sem revelar topologia.</done>
</task>

<task type="auto">
  <name>Task 2: Criar porta Appwrite e diagnóstico desacoplado</name>
  <files>src/leadstream/integrations/appwrite.py, tests/test_appwrite_adapter.py</files>
  <read_first>src/leadstream/common/env.py, src/config/settings/base.py, .planning/phases/01-funda-o-execut-vel/01-RESEARCH.md</read_first>
  <action>Defina uma interface pequena de diagnóstico e um cliente Appwrite configurado apenas por `APPWRITE_ENDPOINT`, `APPWRITE_PROJECT_ID` e `APPWRITE_API_KEY`. Use o endpoint/SDK de health com timeout; traduza resultados para DTO interno sanitizado. Em desenvolvimento sem configuração, retorne `not_configured`; em produção, a ausência deve aparecer no diagnóstico, mas não bloquear readiness. Não implemente TablesDB, models ou storage real nesta fase (D-03).</action>
  <verify>`pytest tests/test_appwrite_adapter.py -q` com mocks passa para sucesso, ausência de configuração, 401 e timeout sem rede real.</verify>
  <acceptance_criteria>Nenhum teste realiza rede; nenhuma exceção expõe header/chave; o módulo Appwrite não é importado por models ou migrations.</acceptance_criteria>
  <done>A instância Appwrite pode ser verificada de modo opcional, seguro e substituível.</done>
</task>

<task type="auto">
  <name>Task 3: Publicar OpenAPI e logging correlacionado</name>
  <files>src/leadstream/common/request_context.py, src/leadstream/common/logging.py, src/config/settings/base.py, src/config/urls.py, tests/test_logging.py</files>
  <read_first>src/config/urls.py, src/leadstream/tenancy/views.py, src/leadstream/common/views.py, .planning/REQUIREMENTS.md</read_first>
  <action>Adicione schema OpenAPI e Swagger/ReDoc sob `/api/v1/schema/` e `/api/v1/docs/`, documentando workspace e health em pt-BR. Crie middleware que aceita ou gera `X-Request-ID`, devolve-o no response e o injeta em logs JSON. Inclua campos preparados para tenant, lote, chunk e provedor e um redactor recursivo para authorization, cookies, passwords, secrets, tokens, API keys, e-mail e telefone. Não registre body bruto (OPS-01, FND-07).</action>
  <verify>`pytest tests/test_logging.py tests/test_workspace_api.py -q` e geração do schema OpenAPI passam.</verify>
  <acceptance_criteria>A documentação referencia `/api/v1/workspace/`; toda resposta testada contém `X-Request-ID`; valores sentinela de segredo/e-mail/telefone não aparecem no log capturado.</acceptance_criteria>
  <done>A API tem contrato navegável e logs seguros correlacionáveis.</done>
</task>

</tasks>

<verification>
- `ruff check .`
- `mypy src`
- `python manage.py spectacular --file /tmp/schema.yml --validate`
- `pytest -q`
- teste automatizado busca valores sentinela e confirma que health/logging não os devolvem.
</verification>

<success_criteria>
- D-08 a D-11 são comprovadas por testes de probes.
- D-03 e FND-05 são atendidos por uma porta Appwrite opcional, sem ORM.
- OPS-01 registra correlação com redação e FND-07 publica OpenAPI `/api/v1` em pt-BR.
</success_criteria>

<output>
After completion, create `.planning/phases/01-funda-o-execut-vel/01-02-SUMMARY.md`.
</output>
