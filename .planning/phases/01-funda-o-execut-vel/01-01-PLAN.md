---
phase: 01-funda-o-execut-vel
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .python-version
  - .gitignore
  - .env.example
  - manage.py
  - src/config/__init__.py
  - src/config/asgi.py
  - src/config/urls.py
  - src/config/settings/__init__.py
  - src/config/settings/base.py
  - src/config/settings/development.py
  - src/config/settings/production.py
  - src/leadstream/__init__.py
  - src/leadstream/common/env.py
  - src/leadstream/tenancy/apps.py
  - src/leadstream/tenancy/models.py
  - src/leadstream/tenancy/migrations/0001_initial.py
  - src/leadstream/tenancy/migrations/0002_seed_internal_tenant.py
  - src/leadstream/tenancy/services.py
  - src/leadstream/tenancy/serializers.py
  - src/leadstream/tenancy/views.py
  - src/leadstream/tenancy/urls.py
  - tests/conftest.py
  - tests/test_workspace_api.py
autonomous: true
requirements:
  - FND-02
  - FND-03
  - FND-04
  - FND-07
user_setup: []
must_haves:
  truths:
    - "A aplicação inicia com configuração exclusiva de ambiente e rejeita configuração de produção incompleta."
    - "Uma migration cria exatamente um tenant interno estável e a API o aplica sem login ou sessão."
    - "GET /api/v1/workspace/ lê o tenant internal do banco e responde em português do Brasil."
    - "Appwrite permanece fora dos models e migrations Django."
  artifacts:
    - path: pyproject.toml
      provides: "Dependências e configuração de qualidade do backend Python"
    - path: src/config/settings/production.py
      provides: "Configuração de produção com validação fail-closed"
    - path: src/leadstream/tenancy/models.py
      provides: "Tenant operacional persistido no PostgreSQL"
    - path: src/leadstream/tenancy/migrations/0002_seed_internal_tenant.py
      provides: "Provisionamento reproduzível do tenant internal"
    - path: src/leadstream/tenancy/views.py
      provides: "Primeira fatia vertical de leitura do workspace"
  key_links:
    - from: src/config/settings/base.py
      to: PostgreSQL
      via: DATABASE_URL
      pattern: "DATABASE_URL"
    - from: src/leadstream/tenancy/views.py
      to: src/leadstream/tenancy/services.py
      via: "resolução automática do tenant internal"
      pattern: "get_internal_tenant"
    - from: src/config/urls.py
      to: src/leadstream/tenancy/urls.py
      via: "/api/v1"
      pattern: "api/v1"
---

<objective>
Criar a fundação executável do backend e provar a primeira fatia vertical: uma migration grava o tenant interno no PostgreSQL e a API versionada o lê sem autenticação de produto.

Purpose: estabelecer o processo Django, as configurações seguras e o isolamento por tenant que todo o domínio futuro reutilizará.
Output: projeto instalável, settings por ambiente, schema inicial, endpoint `/api/v1/workspace/` e testes sem rede externa.
</objective>

<execution_context>
@C:/Users/lxleo/.codex/skills/gsd-execute-phase/SKILL.md
@C:/Users/lxleo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/lxleo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-funda-o-execut-vel/01-CONTEXT.md
@.planning/phases/01-funda-o-execut-vel/01-RESEARCH.md
@.planning/phases/01-funda-o-execut-vel/01-SKELETON.md
</context>

<threat_model>
Assets: credenciais de PostgreSQL, dados futuros por tenant e configuração de produção.
Trust boundaries: ambiente do processo para settings; requisição HTTP para DRF; ORM para PostgreSQL.
Threats and controls:
- Segredo versionado ou exposto: somente variáveis de ambiente, placeholders no exemplo e valores sensíveis excluídos de mensagens.
- Produção iniciada com default inseguro: settings de produção validam segredo, hosts e URL PostgreSQL e falham antes de servir.
- Vazamento entre tenants: serviço central resolve `internal`; modelos de negócio futuros herdarão vínculo obrigatório e consultas tenant-scoped.
- Escrita acidental em Appwrite TablesDB: nenhum SDK Appwrite entra no domínio ou nas migrations deste plano.
Verification: testes de configuração, migration e endpoint; busca no repositório por padrões de segredo real.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Estruturar pacote Python e settings fail-closed</name>
  <files>pyproject.toml, .python-version, .gitignore, .env.example, manage.py, src/config/__init__.py, src/config/asgi.py, src/config/settings/__init__.py, src/config/settings/base.py, src/config/settings/development.py, src/config/settings/production.py, src/leadstream/__init__.py, src/leadstream/common/env.py</files>
  <read_first>.planning/research/STACK.md, .planning/research/PITFALLS.md, .planning/phases/01-funda-o-execut-vel/01-CONTEXT.md</read_first>
  <action>Crie um projeto Django 5.2/DRF em layout `src`, com dependências declaradas no `pyproject.toml`. Implemente um helper tipado para variáveis de ambiente sem imprimir valores. Separe settings base, development e production. Development pode usar defaults locais seguros; production deve exigir `DJANGO_SECRET_KEY`, `DJANGO_ALLOWED_HOSTS` e `DATABASE_URL` PostgreSQL, desabilitar debug, habilitar proxy SSL/cookies seguras e rejeitar SQLite. O `.env.example` deve conter somente placeholders, inclusive Appwrite, RabbitMQ e Redis. Não implemente autenticação, cadastro, sessão ou RBAC (D-06); deixe explícito que o perímetro/gateway protege a implantação interna (D-07). Não copie nenhuma credencial da conversa (D-12, D-13, D-14, D-17).</action>
  <verify>`python -m compileall src manage.py` e um processo com settings de produção sem variáveis obrigatórias deve terminar com erro de configuração sem revelar valor sensível.</verify>
  <acceptance_criteria>`pyproject.toml` declara Django, DRF, psycopg, Celery, RabbitMQ/Redis clients, Appwrite SDK, pytest, Ruff e mypy; `.env.example` não contém host, project ID, senha ou secret reais; `production.py` contém validação explícita de variáveis e `DEBUG = False`.</acceptance_criteria>
  <done>O pacote instala/importa, settings de desenvolvimento carregam e produção falha fechada quando incompleta.</done>
</task>

<task type="auto">
  <name>Task 2: Criar tenant interno por migration</name>
  <files>src/leadstream/tenancy/apps.py, src/leadstream/tenancy/models.py, src/leadstream/tenancy/migrations/0001_initial.py, src/leadstream/tenancy/migrations/0002_seed_internal_tenant.py, src/leadstream/tenancy/services.py</files>
  <read_first>src/config/settings/base.py, .planning/phases/01-funda-o-execut-vel/01-CONTEXT.md, .planning/REQUIREMENTS.md</read_first>
  <action>Implemente `Tenant` com UUID, slug único imutável para uso interno, nome, ativo e timestamps. Crie migrations reproduzíveis: schema e data migration idempotente que provisiona slug `internal`. Centralize `get_internal_tenant()` e uma base abstrata `TenantOwnedModel` para entidades futuras; nenhuma tabela de negócio deve poder existir sem tenant (D-05). A conexão normal usa PostgreSQL nativo via Django ORM, sem Appwrite TablesDB (D-03, D-04).</action>
  <verify>`python manage.py makemigrations --check --dry-run` não gera migration e os testes aplicam migrations a um banco isolado.</verify>
  <acceptance_criteria>A migration `0002_seed_internal_tenant.py` usa `get_or_create`/equivalente histórico; `Tenant.slug` é único; `TenantOwnedModel.tenant` é obrigatório e indexado; nenhum model importa Appwrite.</acceptance_criteria>
  <done>Um banco novo contém exatamente um tenant `internal` após migrations e mantém o mesmo registro em nova execução.</done>
</task>

<task type="auto">
  <name>Task 3: Expor a primeira rota real e testá-la</name>
  <files>src/config/urls.py, src/leadstream/tenancy/serializers.py, src/leadstream/tenancy/views.py, src/leadstream/tenancy/urls.py, tests/conftest.py, tests/test_workspace_api.py</files>
  <read_first>src/leadstream/tenancy/models.py, src/leadstream/tenancy/services.py, .planning/phases/01-funda-o-execut-vel/01-SKELETON.md</read_first>
  <action>Crie `GET /api/v1/workspace/` sem autenticação de produto, resolvendo o tenant interno via serviço e serializando somente id, slug, nome e estado. Use mensagens/erros em pt-BR, namespace/versionamento `api/v1` e resposta estável. Configure pytest-django com banco isolado e teste 200, dados persistidos e ausência de sessão/token exigidos. Não adicione endpoint de escrita de tenant.</action>
  <verify>`pytest tests/test_workspace_api.py -q` passa e confirma que a resposta deriva do banco.</verify>
  <acceptance_criteria>`src/config/urls.py` inclui prefixo exato `api/v1/`; o teste consulta `/api/v1/workspace/`, espera slug `internal` e comprova que o registro existe no ORM.</acceptance_criteria>
  <done>A fatia migration → ORM → serviço → DRF → JSON funciona de ponta a ponta.</done>
</task>

</tasks>

<verification>
- `ruff check .`
- `mypy src`
- `python manage.py makemigrations --check --dry-run`
- `pytest -q`
- busca por `standard_`, senha fornecida e Project ID fornecido retorna zero ocorrências no repositório versionável.
</verification>

<success_criteria>
- Configuração segue D-03 a D-07 e D-12 a D-14/D-17 sem segredos reais.
- PostgreSQL é a fonte de verdade em produção e migrations provisionam o tenant interno.
- `/api/v1/workspace/` entrega uma leitura real e testada em pt-BR.
</success_criteria>

<output>
After completion, create `.planning/phases/01-funda-o-execut-vel/01-01-SUMMARY.md`.
</output>
