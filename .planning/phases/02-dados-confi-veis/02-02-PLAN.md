---
phase: 02-dados-confi-veis
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/config/settings/base.py
  - src/leadstream/evidence/__init__.py
  - src/leadstream/evidence/apps.py
  - src/leadstream/evidence/models.py
  - src/leadstream/evidence/services.py
  - src/leadstream/evidence/migrations/__init__.py
  - src/leadstream/evidence/migrations/0001_initial.py
  - src/leadstream/evidence/migrations/0002_observation_append_only.py
  - tests/test_evidence_models.py
  - tests/test_canonicalization.py
autonomous: true
requirements:
  - DATA-04
  - DATA-05
  - DATA-06
  - COMP-01
must_haves:
  truths:
    - "D-10 D-11: cada observação tem estado, fonte, instante, método, evidência, confiança, expiração e finalidade."
    - "D-12: Observation é append-only na aplicação e por trigger PostgreSQL."
    - "D-13 D-14 D-15: decisão canônica versionada aponta para observação elegível por ranking determinístico sem relabeling."
    - "D-16: conflitos e recanonizações preservam participantes e decisões anteriores."
    - "D-17: SourceRecord exige finalidade profissional e política de retenção."
    - "D-24 D-25: canonização usa transação/lock e semântica crítica é testada em PostgreSQL."
  artifacts:
    - path: src/leadstream/evidence/models.py
      provides: "fonte, registro, evidência, observação, conflito e decisão canônica"
    - path: src/leadstream/evidence/services.py
      provides: "append e seleção/recanonização determinística"
    - path: src/leadstream/evidence/migrations/0002_observation_append_only.py
      provides: "proteção PostgreSQL contra update/delete"
  key_links:
    - from: src/leadstream/evidence/models.py
      to: src/leadstream/entities/models.py
      via: "Observation.target FK para Entity"
      pattern: "ForeignKey.*Entity"
    - from: src/leadstream/evidence/services.py
      to: CanonicalDecision
      via: "ranking sob select_for_update"
      pattern: "select_for_update"
---

<objective>
Implementar a trilha append-only de proveniência, os conflitos e a seleção canônica determinística.

Purpose: impedir que resultados encontrados, inferidos ou tecnicamente validados sejam promovidos
sem o estado e a evidência corretos.
Output: app `evidence`, trigger PostgreSQL, serviços de canonização e testes.
</objective>

<execution_context>
@C:/Users/lxleo/.codex/skills/gsd-execute-phase/SKILL.md
@C:/Users/lxleo/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/lxleo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-dados-confi-veis/02-CONTEXT.md
@.planning/phases/02-dados-confi-veis/02-RESEARCH.md
@.planning/phases/02-dados-confi-veis/02-PATTERNS.md
@.planning/phases/02-dados-confi-veis/02-01-SUMMARY.md
</context>

<threat_model>
Assets: proveniência, histórico de decisão, finalidade de tratamento e integridade de evidências.
Trust boundaries: provedores futuros → append service; aplicação → PostgreSQL; canonização → consumidores.
Threats and controls:
- Reescrita de histórico: bloqueio em model/QuerySet e trigger PostgreSQL para update/delete.
- Elevação fraudulenta de inferência: enum explícito, política de elegibilidade e decisão referencia estado original.
- Canonização concorrente: lock da entidade/campo, constraint de versão e transação atômica.
- Evidência sem finalidade: SourceRecord exige purpose/retention e valida tenant.
Verification: testes de imutabilidade, ranking, desempate, concorrência e conflito/recanonização.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Modelar origem, evidência e observação</name>
  <files>src/config/settings/base.py, src/leadstream/evidence/__init__.py, src/leadstream/evidence/apps.py, src/leadstream/evidence/models.py, src/leadstream/evidence/migrations/__init__.py, src/leadstream/evidence/migrations/0001_initial.py, tests/test_evidence_models.py</files>
  <read_first>src/leadstream/entities/models.py, .planning/phases/02-dados-confi-veis/02-CONTEXT.md, .planning/phases/02-dados-confi-veis/02-RESEARCH.md</read_first>
  <action>Crie `ProcessingPurpose`, `RetentionPolicy`, `Source`, `SourceRecord`, `Evidence`, `Observation`, `Conflict`, associação de participantes e `CanonicalDecision`. `Observation` referencia `Entity`, field_path, value_json/fingerprint, enum ABSENT/OBSERVED/INFERRED/TECHNICALLY_VALIDATED/CONFIRMED/CONFLICTING/REJECTED, confiança 0–100, observed/captured/expires, método e supersedes. SourceRecord exige purpose e retention. Evidence guarda URL/identificador e excerpt ou hash minimizado. Inclua constraints, tenant direto onde necessário e validação cruzada (D-10, D-11, D-13, D-16, D-17, D-22).</action>
  <verify>`pytest tests/test_evidence_models.py -q` e `python manage.py makemigrations --check --dry-run`</verify>
  <acceptance_criteria>Confiança fora de 0–100 falha; observação não pode apontar Entity/SourceRecord de tenant diferente; decisão aponta Observation; fonte possui prioridade; source record sem purpose/retention falha.</acceptance_criteria>
  <done>Schema representa toda proveniência e não mistura valor observado com decisão canônica.</done>
</task>

<task type="auto">
  <name>Task 2: Garantir append-only no app e PostgreSQL</name>
  <files>src/leadstream/evidence/models.py, src/leadstream/evidence/migrations/0002_observation_append_only.py, tests/test_evidence_models.py</files>
  <read_first>src/leadstream/evidence/models.py, src/config/settings/test.py</read_first>
  <action>Impeça `save(update_fields=...)`, `QuerySet.update`, `delete` de instância e `QuerySet.delete` para Observation. Crie migration `RunPython`/`RunSQL` vendor-guarded que instala trigger PostgreSQL `BEFORE UPDATE OR DELETE`, com reverse explícito. SQLite deve continuar migrável, mas a CI PostgreSQL prova o trigger. Correções adicionam nova observação via `supersedes` (D-12, D-25).</action>
  <verify>Teste app-level em SQLite e teste marcado PostgreSQL que tenta SQL UPDATE/DELETE direto e espera erro</verify>
  <acceptance_criteria>Nenhum caminho ORM normal altera/apaga observação; trigger existe após migration PostgreSQL e rollback remove função/trigger; nova observação supersedente é permitida.</acceptance_criteria>
  <done>Histórico é append-only em duas camadas sem inviabilizar desenvolvimento local.</done>
</task>

<task type="auto">
  <name>Task 3: Implementar append, conflito e canonização determinística</name>
  <files>src/leadstream/evidence/services.py, tests/test_canonicalization.py</files>
  <read_first>src/leadstream/evidence/models.py, src/leadstream/entities/services.py, .planning/phases/02-dados-confi-veis/02-RESEARCH.md</read_first>
  <action>Implemente `append_observation`, `open_conflict` e `canonicalize`. Valide tenant e estado, compute JSON/fingerprint, use transaction.atomic e lock por target/field. Exclua ABSENT/REJECTED/CONFLICTING, expirados e marcados como suprimidos; ordene por peso de estado, prioridade Source, confiança, observed_at e UUID. Grave nova CanonicalDecision com versionamento e policy_version, mantendo decisões anteriores. INFERRED selecionado continua INFERRED. Recanonicalização nunca edita decisão anterior (D-13..D-16, D-19, D-20, D-24).</action>
  <verify>`pytest tests/test_canonicalization.py -q`</verify>
  <acceptance_criteria>Execuções com mesma entrada escolhem a mesma observação; fonte prioritária vence apenas depois da elegibilidade; inferência nunca retorna CONFIRMED; conflito preserva participantes; recanonização incrementa versão.</acceptance_criteria>
  <done>Seleção canônica é reproduzível, auditável e segura sob concorrência.</done>
</task>

</tasks>

<verification>
- `ruff check .`
- `mypy src`
- `python manage.py makemigrations --check --dry-run`
- `pytest tests/test_evidence_models.py tests/test_canonicalization.py -q`
- CI PostgreSQL executa teste do trigger append-only.
</verification>

<success_criteria>
- DATA-04, DATA-05, DATA-06 e COMP-01 têm testes executáveis.
- D-10 a D-17, D-24 e D-25 são aplicados sem atalhos de confiança.
- Observação, conflito e decisão permanecem auditáveis após recanonização.
</success_criteria>

<output>
After completion, create `.planning/phases/02-dados-confi-veis/02-02-SUMMARY.md`.
</output>
